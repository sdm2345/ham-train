> 噪声指数NF(dB) = **10lg(Fn)**，是噪声系数Fn的**对数形式**

| 参数 | 形式 | 关系 |
|------|------|------|
| Fn | 比值 | 无单位 |
| NF | 对数 | NF(dB) = 10lg(Fn) |
| 定义 | 信噪比比值的对数 | 10lg(Si/Ni ÷ So/No) |

**对比A-570**：A-570讲Fn定义(比值)，A-573讲NF定义(对数)