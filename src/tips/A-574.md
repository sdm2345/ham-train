> NF必**>0dB**；NF越**接近0**越好（理想值=0dB），对应Fn=1

| 噪声指数 | 特性 | 判断 |
|---------|------|------|
| NF值域 | 必须>0dB | 对应Fn>1 |
| NF=0dB | 理想放大器 | 对应Fn=1 |
| NF↓ | 性能↑ | **越接近0dB越好** |
| NF与Fn | 对数转换 | NF(dB)=10lg(Fn) |

**换算例**：Fn=2 → NF=3dB；Fn=10 → NF=10dB；**更小更好**原则相同